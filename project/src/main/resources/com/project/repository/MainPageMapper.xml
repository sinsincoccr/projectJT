<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.repository.MainPageMapper">

    <!-- 회원 가입 처리 -->
    <insert id="editer">
        select * from job_offer;
    </insert>

    <insert id="thumbnailer">
        select * from job_offer;
    </insert>

    <insert id="sdCharacter">
        select * from job_offer;
    </insert>

    <insert id="virtual">
        select * from job_offer;
    </insert>

    <insert id="camaraMan">
        select * from job_offer;
    </insert>


</mapper>